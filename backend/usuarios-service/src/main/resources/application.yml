server:
  port: 8081

spring:
  application:
    name: usuarios-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/usuarios_db}
    username: ${DB_USERNAME:rocker}
    password: ${DB_PASSWORD:rocker123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  jackson:
    default-property-inclusion: non_null

jwt:
  secret: NPQV4SOF9JGL7gUwFFUXy_68nLyjj9kMOPckZl2gZF6XbFE9NI0xKJOwl3bey-LB
  expiration: 86400000

viacep:
  url: https://viacep.com.br/ws

springdoc:
  swagger-ui:
    path: /swagger-ui.html